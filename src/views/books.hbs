<table class="table">
    <thead>
    <tr>
        <th>#</th>
        <th>Title</th>
        <th>Authors</th>
        <th>ISBN</th>
    </tr>
    </thead>
    <tbody>
    {{#books}}
        <tr>
            <th></th>
            <td><a href="{{url}}">{{title}}</a></td>
            <td>{{authors}}</td>
            <td>{{isbn}}</td>
        </tr>
    {{/books}}
    </tbody>
</table>
<nav aria-label="navigation between pages with books">
    <ul class="pagination">
        {{#pages}}
            <li class="page-item {{#isCurrent}}active{{/isCurrent}}">
                <a class="page-link" href="{{href}}">{{humanDisplay}}</a>
            </li>
        {{/pages}}
    </ul>
</nav>